{
  "name": "inline-css-imports",
  "version": "0.2.1",
  "description": "Finds, requests and inlines all css @imports in a css string",
  "main": "lib/index.js",
  "scripts": {
    "prepublish": "./node_modules/.bin/babel -d lib src/",
    "pretest": "./node_modules/.bin/babel -d lib src/",
    "test": "ava -v --timeout 5s"
  },
  "keywords": [
    "css",
    "css",
    "imports",
    "node"
  ],
  "author": "Jonas Ohlsson",
  "license": "ISC",
  "engines": {
    "node": ">=0.12.0"
  },
  "devDependencies": {
    "ava": "^0.16.0",
    "babel": "^5.8.23",
    "babel-core": "^5.8.25",
    "babel-preset-es2015": "^6.1.2",
    "st": "^1.0.0"
  },
  "dependencies": {
    "node-fetch": "^3.1.1"
  }
}
